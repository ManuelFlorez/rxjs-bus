{
  "name": "rxjs-bus",
  "version": "0.0.1",
  "description": "A simple RxJS event bus",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "ts-node --project tsconfig.dev.json --esm test/busTest.ts",
    "test1": "ts-node --project tsconfig.dev.json --esm test/commandBusTest.ts",
    "test2": "ts-node --project tsconfig.dev.json --esm test/domainEventTest.ts",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "keywords": ["rxjs", "bus", "event"],
  "author": "Manuel Florez",
  "dependencies": {
    "rxjs": "^7.8.2",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
